[project]
name = "lians-agent-memory-mcpb"
version = "0.5.0"
description = "Local MCPB launcher for Lians agent memory"
requires-python = ">=3.10"
dependencies = ["lians-sdk[mcp,local]==0.5.0"]

[tool.uv]
package = false
