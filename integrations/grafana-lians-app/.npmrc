# Grafana and React modules are supplied by the Grafana host at runtime. The
# plugin compiles against narrow local declarations and externalizes them from
# its SystemJS bundle, so installing peer implementations only adds unrelated
# host dependency risk to CI.
legacy-peer-deps=true
