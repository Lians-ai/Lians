apiVersion: v1
kind: Secret
metadata:
  name: lians-metrics-credentials
  namespace: monitoring
type: Opaque
stringData:
  # Must exactly match METRICS_BEARER_TOKEN in the application Secret.
  token: CHANGE_ME_random_32_plus_character_metrics_token
