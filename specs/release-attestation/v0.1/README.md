# Lians Release Attestation v0.1

A signed release authorization commitment binding an eligible Evaluation
Attestation, immutable AgentVersion and environment manifest, and the exact
unexpired Gate approval attestations required by policy. Rollout evidence is
recorded separately as shadow, canary, and production deployments so an issued
attestation is never confused with a completed deployment.
