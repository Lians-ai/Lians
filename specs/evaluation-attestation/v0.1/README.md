# Lians Evaluation Attestation v0.1

A signed, portable commitment to one immutable baseline-versus-candidate
comparison. The hash-covered payload pins the suite, runs, agent versions,
repeated-trial inventory, scorer provenance, aggregates, protected metrics,
critical invariants, verdict, claims, and limitations. An attestation is
evidence for human release review; it does not deploy or approve a change.
